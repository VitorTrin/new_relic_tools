defmodule NewRelicToolsTest do
  use ExUnit.Case
  doctest NewRelicTools

  # No tests for now.
  # New relic agent isn't very helpful with testing,
  # I can't even test if an attribute is present with public functions.
end
